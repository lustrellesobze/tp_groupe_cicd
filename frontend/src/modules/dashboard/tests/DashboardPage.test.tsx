import { describe, expect, it } from 'vitest';

describe('Module Dashboard — DashboardPage', () => {
  it('stub: affiche les statistiques', () => {
    // TODO: implémenter avec mock API
    expect(true).toBe(true);
  });
});
