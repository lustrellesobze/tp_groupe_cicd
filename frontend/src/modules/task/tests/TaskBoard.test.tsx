import { describe, expect, it } from 'vitest';

describe('Module Task — TaskBoard', () => {
  it('stub: affiche les colonnes du kanban', () => {
    // TODO: implémenter avec mock DnD + données
    expect(true).toBe(true);
  });
});
