import { describe, expect, it } from 'vitest';

describe('Module Project — ProjectsPage', () => {
  it('stub: affiche la liste des projets', () => {
    // TODO: implémenter avec mock API
    expect(true).toBe(true);
  });
});
